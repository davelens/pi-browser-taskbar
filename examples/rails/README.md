# Rails example

This directory is owned by the examples module and is reserved for the later executable Rails
example. Built-gem conformance now proves the conventional ERB integration; the future example will
consume the same public `pi-browser-taskbar-rails` seams without example-only runtime code.
