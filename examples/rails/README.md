# Rails example

This directory is owned by the examples module and is reserved for the conventional Rails example.
It will consume only the public `pi-browser-taskbar-rails` package seam. The executable application
arrives with the Rails tracer bullet; no example-only runtime belongs here.
