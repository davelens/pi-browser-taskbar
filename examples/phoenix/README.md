# Phoenix example

This directory is owned by the examples module and is reserved for the conventional Phoenix
example. It will consume only the public `pi_browser_taskbar_phoenix` package seam. The executable
application arrives with the Phoenix tracer bullet; no example-only runtime belongs here.
